#!/usr/bin/env bash

# This is a shell script example for the shellexecutor plugin
# that simply dumps the execution shell environment to the log.
# The output should be visible at DEBUG

echo "Command Environment:"
env

exit 0;
